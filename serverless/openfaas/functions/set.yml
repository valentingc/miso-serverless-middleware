version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  set:
    lang: node18
    handler: ./set
    image: localhost:4000/set
    environment:
      NODE_ENV: production
    labels:
      com.openfaas.scale.min: 5
      com.openfaas.scale.max: 5

